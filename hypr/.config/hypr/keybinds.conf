$mainMod = SUPER # Sets "Windows" key as main mod
$terminal = kitty
$fileManager = dolphin
$menu = hyprlauncher
$editor = code

bind = $mainMod, RETURN, exec, $terminal
bind = $mainMod, Q, killactive,
bind = $mainMod, S, global, quickshell:clipboardToggle
bind = $mainMod SHIFT, S, global, quickshell:screenshotToggle
bind = $mainMod, C, exec, code
bind = $mainMod, M, exec, command -v hyprshutdown >/dev/null 2>&1 && hyprshutdown || hyprctl dispatch exit
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, V, togglefloating,
bind = $mainMod, R, global, quickshell:launcherToggle
bind = $mainMod, P, pseudo # dwindle
bind = $mainMod, J, layoutmsg, togglesplit # dwindle
bind = $mainMod, Y, exec, yazi
bind = $mainMod, W, exec, firefox
bind = $mainMod SHIFT, W, exec, brave

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace [1-10] and switch to it
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Scroll through workspaces
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow # LMB = left mouse button = 272
bindm = $mainMod, mouse:273, resizewindow # RMB = right mouse button = 273

# Shortcuts
binde = , F3, exec, wpctl set-volume -l 1.0 @DEFAULT_AUDIO_SINK@ 5%+ # Vol-up
binde = , F2, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%- # Vol-down
binde = , F1, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ # Mute/Unmute

binde = , F5, exec, brightnessctl set 5%+ # Brightness +
binde = , F4, exec, brightnessctl set 5%- # Brightness -

# Keyboard lit
bind = , F7, exec, brightnessctl --device="asus::kbd_backlight" set $(( ( $(brightnessctl --device="asus::kbd_backlight" get) + 1 ) % ( $(brightnessctl --device="asus::kbd_backlight" max) + 1) ))

# Lock Screen
bind = $mainMod, L, exec, hyprlock
bind = , F9, exec, hyprlock

# Zenyyxz-Agent
bind = $mainMod, Z, exec, kitty --title "Zenyyxz Agent" /home/zenyyxz/Documents/Projects/whitehatagent/launch.sh

